# Fixtures

`de_ancient-2026-05-17/` is a legacy v1 export retained as a real-world sample.

Strict v2 fixtures must use `schemaVersion: "cs2-demo-format/2.0"`. The fixture
validation script skips legacy fixtures until a v2 golden export is generated by
the producer.
