# Fixtures

## v3-mid

Golden fixture for cs2-demo-format v3.

- **Demo**: de_anubis, ~343 MB .dem, 21 formal rounds (12-9)
- **Export profile**: research (`--research` — includes `duels.json`)
- **Schema version**: `cs2-demo-format/3.0`
- **Generated by**: `cs2df` (Python reference exporter in `python/`)
- **Contents**: all 12 required files + `shots.json` + `replay.json` + `duels.json`

Regenerate with:

```bash
cd python && uv run cs2df export \
  ../cs2-demo-analysis-kit/fixtures/_bench/mid.dem \
  -o ../fixtures/v3-mid.zip --research
```

Then extract into the fixture directory to update checked-in files.

## de_ancient-2026-05-17

Legacy v1 fixture (`schemaVersion: cs2-demo-format/1.0`). Skipped by
`pnpm validate:fixtures`. Retained as a format-history sample.
